---
import Layout from "./Layout.astro";
export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<Layout title={title}>
  <main class="w-full h-full flex flex-col px-10 pt-12">
    <slot />
  </main>
</Layout>
