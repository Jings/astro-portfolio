---
import Layout from "./Layout.astro";
export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<Layout title={title}>
  <main
    class="w-full h-full flex flex-col md:flex-row px-10 items-center justify-center gap-10"
  >
    <slot />
  </main>
</Layout>
